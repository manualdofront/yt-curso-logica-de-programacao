programa {
  funcao inicio() {
    cadeia dias[] = {"Dom", "Seg", "Ter", "Qua", "Qui", "Sex", "Sáb"}

    // Dica: em linguagens como Javascript, você usaria o tamanho do array
    // Exemplo: for (i=0; i<dias.length; i++)
    para (inteiro i=0; i<7; i++) {
      escreva(dias[i], "\n")
    }
  }
}
